RUN_MODE = prod
RUN_USER = git

[attachment]
PATH = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/attachments

[database]
PATH = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/gitea.db

[indexer]
ISSUE_INDEXER_PATH = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/indexers/issues.bleve
REPO_INDEXER_PATH = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/indexers/repos.bleve

[log]
MODE = file
ROOT_PATH = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/log

[picture]
AVATAR_UPLOAD_PATH = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/avatars

[repository]
ROOT = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/repositories

[repository.upload]
TEMP_PATH = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/uploads

[server]
DISABLE_ROUTER_LOG = true
LFS_CONTENT_PATH = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/lfs
SSH_PORT = 2222
START_SSH_SERVER = true

[session]
PROVIDER_CONFIG = ⁅⁅ .Env.GITEA_WORK_DIR ⁆⁆/data/sessions
